<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dmdulliustisolutions.meuapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.CadastroEventosActivity"></activity>
        <activity
            android:name=".Views.ListaEventosActivity"
            android:label="@string/title_activity_lista_eventos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Views.CalculadoraActivity" />
        <activity android:name=".Views.MenuActivity" />
        <activity android:name=".Views.Mega_senaActivity" />
        <activity android:name=".Views.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.MainActivity" />
    </application>

</manifest>